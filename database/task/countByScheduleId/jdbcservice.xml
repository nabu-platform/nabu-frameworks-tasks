<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<sql>select count(id) as total from tasks where task_schedule_id = :scheduleId
	and (:scheduled is null or scheduled = :scheduled)</sql>
</jdbcService>