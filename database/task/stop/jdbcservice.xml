<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<sql>update ~tasks set
	modified = current_timestamp,
	stopped = current_timestamp,
	state = :state,
	task_output = :taskOutput
where id = :id
and state = 'RUNNING'
and owner = :owner</sql>
</jdbcService>