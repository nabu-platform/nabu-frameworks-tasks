<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<sql>select
	id,
	created,
	modified,
	target,
	schedule,
	until,
	amount,
	allow_overlap,
	task,
	task_type,
	task_uri,
	enabled,
	task_queue_id
from task_schedules
where task_queue_id = :queueId
and enabled = true</sql>
	<outputDefinition>nabu.frameworks.tasks.types.model.tasks.TaskSchedule</outputDefinition>
</jdbcService>